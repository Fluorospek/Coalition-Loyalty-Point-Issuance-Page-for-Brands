declare module 'neucron-sdk' {
    // If you know the structure of the module, you can add specific typings here.
    // For example:
    // export function someFunction(param: string): void;
  
    // If you don't know the structure, you can declare it as `any`.
    const neucronSdk: any;
    export default neucronSdk;
  }